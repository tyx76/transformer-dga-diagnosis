# 油浸式电力变压器 DGA 故障根因分析智能体

基于 RAG（检索增强生成）的工业设备故障诊断系统：用户输入故障现象（如"变压器油中乙炔含量超标"），系统从规程知识库检索依据，由大模型生成**可溯源**的根因分析与合规处置建议。

- 对象：输变电/工业厂站中的**油浸式电力变压器（主变）**
- 诊断主线：**DGA（油中溶解气体分析）**，判据依 **DL/T 722-2014**
- 考核：四川大学未来技术创新创业社团考核 · 考题八（技术向）；评比 2026-10-12 ~ 10-16

## 目录结构
| 目录 | 内容 |
|---|---|
| `docs/` | 项目状态（00）、文献清单（01/05/07/08）、概念与路线（02/03）、任务二方案（04）、精读笔记（06） |
| `data/` | `语料/`（规程正文，本地保留不入库）、`知识库/`（规则 JSON：DL/T 572 第7章、DL/T 722 判据） |
| `scripts/` | 语料处理与诊断脚本 |
| `venv/` | 本地虚拟环境（不入库） |
| `原始资料/` | 考核题目等原始材料（本地保留不入库） |
| `文献/` | 参考文献 PDF（本地保留不入库，版权/体积原因） |

## 环境要求
- Python 3.10+（本机实测 3.14.7）
- Windows PowerShell

## 快速开始
```powershell
# 1) 创建并激活虚拟环境
python -m venv venv
.\venv\Scripts\Activate.ps1

# 2) 安装依赖
pip install -r requirements.txt

# 3) 配置密钥（新建 .env，不要提交）
# DEEPSEEK_API_KEY=你的key
```

> Windows 中文路径提示：命令行直接传中文路径可能乱码。建议先 `cd` 到项目根目录，再用相对路径执行（如 `.\venv\Scripts\python.exe scripts\env_check.py`）。

## 数据与版权说明

> 详见 [NOTICE.md](NOTICE.md)：本仓库不收录标准原文与第三方原始数据文件。
- `文献/`（标准/论文 PDF）与 `data/语料/*.txt`（标准正文转写）**含版权内容，已被 .gitignore 排除**，仅本地使用。
- 仓库内保留自有产物：docs 文档、规则 JSON、代码。

## 团队分工
- 队长：环境/架构/Agent 工程
- 成员2：领域知识、数据录入、规则库
- 成员3：文献综述、检索与评测、文档

## 当前进度
见 [`docs/00_项目状态与进度.md`](docs/00_项目状态与进度.md)。